services:
  frontend:
    image: csbs1-frontend:latest
    ports:
      - "80:80"
    networks:
      - common-network

  backend:
    image: csbs1-backend:latest
    ports:
      - "3000:3000"
    networks:
      - common-network

networks:
  common-network:
    driver: bridge